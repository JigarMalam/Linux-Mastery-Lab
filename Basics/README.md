1. Linux is the kernel, GNU gives tools, together they form the operating system called GNU/Linux.
2. Linux is open source: anyone can see, change, and share the code—this is why it grew so fast.
3. git config --global alias.lazy "!f() { git add . && git commit -m \"${1:-auto}\" && git push origin main; }; f"
4. A Linux distribution is Linux + tools + apps bundled into a complete usable system.
	-> A Linux distribution is a complete operating system made by bundling the Linux kernel with GNU tools, applications, installers, and a package manager.
	-> Linux Kernel
		+ GNU Tools
		+ Applications (Browser, Mail, etc.)
		+ Installer & Package Manager
		= Linux Distribution
5. 


